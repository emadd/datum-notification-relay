"""datum-notification-relay: a generic, anonymous push-relay server.

Stores only {job schedule, job kind, an anonymous key, an APNs device token}.
Never sees or stores tracker data, values, names, or notes from any client app.
"""
