# Local Terraform working state — never commit
.terraform/
*.tfvars
*.tfvars.json
crash.log
crash.*.log

# Secrets — the APNs .p8 key must never land in this repo
*.p8
.env

# Build artifacts (Lambda zips + the dependency layer) — regenerated by
# scripts/build_lambda_layer.sh + terraform's archive_file data source.
build/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
